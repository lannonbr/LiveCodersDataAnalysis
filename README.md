# LiveCoders polling function

A Lambda function that will trigger every 5 minutes and check out of all of the [LiveCoders](https://livecoders.dev) who are live and insert entries into a DynamoDB table.
